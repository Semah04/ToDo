# MongoDB Connection String
# Format: mongodb://[username:password@]host[:port][/database][?options]
# For local MongoDB: mongodb://localhost:27017/todoapp
# For MongoDB Atlas (cloud): mongodb+srv://username:password@cluster.mongodb.net/todoapp
MONGODB_URI=mongodb://localhost:27017/todoapp

# Server Port
# The port where the backend API will run
PORT=3001

# CORS Origins (comma-separated list)
# Allowed frontend origins that can access this API
CORS_ORIGINS=http://localhost:3000,http://localhost:3002

